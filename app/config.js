    //config.js

    const config = {

        BOARD_SIZE: 24,
        CARDS_PER_ROW: 4,
        
        GAME_STATE_NO_TURNED_CARD: 0,
        GAME_STATE_1_TURNED_CARD: 1,
        GAME_STATE_2_TURNED_CARD: 2,
        GAME_STATE_GAME_OVER: 3,

        CARD_STATE_IN_GAME: 0,
        CARD_STATE_IN_OVER: 1,

        CARD_INVISBLE: 'oi oi-zoom-in text-primary',
    };

    module.exports = config;